#include <iostream>
 
using namespace std;
 
int main() {
 
    char c;
    string s;
    int i, j, len, count;

    while (cin>>c)
    {
        cin>>s;
        len = s.size();
        //cout<<"the size is: "<<len;
        count = 0;
        if (c == '0' && s[0] == '0')
        {
           break;
        }
        
    }
    

    return 0;
}